name: variant-pipeline
channels:
  - conda-forge
  - bioconda
dependencies:
  - python=3.11
  - bwa
  - samtools
  - bcftools
  - fastp
  - htslib
  # tabix/bgzip ship with htslib — do not list tabix separately (avoids dual-resolve conflicts)
  # Python stages use stdlib only (no pandas/jinja2)
